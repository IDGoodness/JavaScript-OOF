// A function to calculate the logarithm of a number

function log(num) {
    let num_log = Math.log(num)
    return num_log
}

result = log(10)
console.log("The logarithm of the number is: " +result)