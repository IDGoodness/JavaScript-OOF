// Mod -- A function to do the remainder calculation with JavaScript

function mod(x, y) {
    let modRem = x % y
    return modRem

}

let result = mod(10, 3);
console.log("The remainder is: " +result);
